<div class="form-group mt-3" >
  <%= form_for(:message, remote: true) do |form| %>
     <div class="field" >
      <%= form.text_field :body,id:"form_message" %>
    </div>
    <div class="form-group mt-4" >
      <%= form.submit "Save", class: "btn btn-primary me-1" %>
    </div>
  <% end %>
</div>