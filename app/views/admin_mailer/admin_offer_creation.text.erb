New offer
==========================================================

Offer n° <%= @real_estate.id %>
    <% user_id = @real_estate.user_id %>
    <% real_estate_id = @real_estate.id %>
    Seller :      <%= User.find(user_id).email %>
    Price :       <%= @real_estate.price %> €
    Address :     <%= @real_estate.adress %>
    Zipcode :     <%= @real_estate.zipcode %>
    City :        <%= @real_estate.city %>
    Description : <%= @real_estate.description %>