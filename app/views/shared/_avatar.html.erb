<% if @avatar_data[:user].avatar.attached? %>
    <%= image_tag @user.avatar, 
                    alt: 'avatar', 
                    class: @avatar_data[:class] %>
<% else %>
    <%= inline_svg_tag "profile.svg", 
                    class: 'default-avatar ' + @avatar_data[:class] %>
<% end %>